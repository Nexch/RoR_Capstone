
  <div class="d-flex flex-wrap" style="height: 100vh;">
    <div class=" bg-dark  text-white p-2" style="width: 18rem;">
      <div class="d-flex align-items-center justify-content-around ">
      <img src="https://static.comicvine.com/uploads/original/11111/111113706/5508193-7637959662-40788.jpg" class="card-img-top rounded-circle " alt="..." style="width: 5rem; height: 5rem;">
      <h5 class="card-title"><%= current_user.name%></h5>
      </div>
      <div class=" d-flex align-items-baseline justify-content-around">
        <div class="card-body">
          <p> Follows <p>
          <p class="card-text text-white"><%= current_user.follows.count%></p>
        </div>
        <div class="mr-5">
          <p> Follower<p>
          <p> <% lol = Follow.all%>
              <% count = 0 %>
              <% lol.each do |meme|%>
                <% if meme.friend_id == current_user.id%>
                  <% count += 1%>
                <% end %>
              <% end %>
              <%= count %>
          </p>
        </div>
      </div>
        <div class="">
            <ul class="list-group list-group-flush">
              <li class="list-group-item bg-dark"><%= link_to 'New Tweet', new_tweet_path %></li>
              <li class="list-group-item bg-dark"><%= link_to 'User page', user_path(current_user) %></li>
              <li class="list-group-item bg-dark"><%= link_to "logout", destroy_user_session_path, method: :delete  %></li>
              <li class="list-group-item bg-dark"><%= link_to "delete", user_registration_path, method: :delete, data: { confirm: "You sure?" } %></li>

            </ul>

        </div>
    </div>


    <div class="d-flex flex-column flex-fill">
      <nav class="navbar navbar-expand-lg navbar-dark bg-primary m-0">
        <a class="navbar-brand" href="/"><img src="https://i.pinimg.com/736x/0a/c8/e8/0ac8e8ab266076f9d654e8387ba5a993.jpg" class="rounded-circle " alt="..." style="width: 2rem; height: 2rem;">
 </a>
      </nav>
      <div class="d-flex justify-content-between" >
        <div class="d-flex flex-column" style="width: 100%;">
          <p id="notice"><%= notice %></p>

          <h1><%= User.find(params[:id]).name%>Tweets</h1>
          <div class="">
            <%= form_with model: @friendship, url: follows_path do |f| %>
              <div class="field">
                <%= f.hidden_field :friend_id, :value => params[:id] %>
                <%= f.submit 'Follow' %>
              </div>
            <% end %>
          </div>

<table class="text-wrap ml-5 mr-5"style=" word-wrap: break-word; ">
  <thead>

    <style>
      table {
        border-collapse: collapse;
        table-layout: fixed;
        width: 310px;
      }
      table td {
        border-bottom: solid 1px #666;
        width: 20rem;
        word-wrap: break-word;
        margin-bottom: 50px;
      }
    </style>

  </thead>

  <tbody>
  <% lol = Tweet.all  %>
  <tr class="d-flex flex-column">
  <% lol.each do |meme| %>
    <% if meme.user_id.to_i == params[:id].to_i%>
      <td><%= meme.tweet %> </td><br>
    <% end %>
  <% end %>
  </tr>
  </tbody>
</table>





</div>
      </div>
    </div>
